<template>
  <div id="app">
    <router-view/>
  </div>
</template>

<style lang="scss">
  // 虽然在vue.config.js里配置过了全局样式，但是页面里没有需要单独写的样式，也需要使用style标签，并且至少有一行样式（//注释也可以）
  // 在这里占位也可以
  // @import '@/style/_index.scss';
</style>
